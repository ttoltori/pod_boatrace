<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pengkong.boatrace.mybatis.client.ProcedureExecutorMapper">
  <update id="executeSql" parameterType="String" statementType="CALLABLE">
  	{call execute_sql (
  		#{paramSql, jdbcType = VARCHAR, mode = IN}
  	)}
  </update>
  <update id="executeOutputWekaPattern" parameterType="hashmap" statementType="CALLABLE">
  	{call output_weka_pattern(
  		#{paramDescription, jdbcType = VARCHAR, mode = IN},
  		#{paramBettype, jdbcType = VARCHAR, mode = IN},
  		#{paramKumiban, jdbcType = VARCHAR, mode = IN},
  		#{paramPatternName, jdbcType = VARCHAR, mode = IN},
  		#{paramPatternValue, jdbcType = VARCHAR, mode = IN},
  		#{paramMinimumBetCount, jdbcType = INTEGER, mode = IN},
  		#{paramMetricBiasPercent, jdbcType = FLOAT, mode = IN},
  		#{paramFromYmd, jdbcType = VARCHAR, mode = IN},
  		#{paramToYmd, jdbcType = VARCHAR, mode = IN}
  	)}
  </update>
  <update id="executeFinalizeWekaPattern" parameterType="hashmap" statementType="CALLABLE">
  	{call finalize_weka_kumiban(
  		#{paramDescription, jdbcType = VARCHAR, mode = IN},
  		#{paramBettype, jdbcType = VARCHAR, mode = IN},
  		#{paramKumiban, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentIncomeAmtStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentHitRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentIncomeRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentMinusDaysRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentIncomeAmtSlopeStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentRecoverPlusRateStr, jdbcType = VARCHAR, mode = IN}
  	)}
  </update>
  <update id="executeFinalizeManualPattern" parameterType="hashmap" statementType="CALLABLE">
  	{call finalize_manual_pattern(
  		#{paramDescription, jdbcType = VARCHAR, mode = IN},
  		#{paramBettype, jdbcType = VARCHAR, mode = IN},
  		#{paramKumiban, jdbcType = VARCHAR, mode = IN}
  	)}
  </update>
  <update id="executeAnalyzeWekaPattern" parameterType="hashmap" statementType="CALLABLE">
  	{call analyze_weka_pattern(
  		#{paramDescription, jdbcType = VARCHAR, mode = IN},
  		#{paramBettype, jdbcType = VARCHAR, mode = IN},
  		#{paramKumiban, jdbcType = VARCHAR, mode = IN},
  		#{paramPatternName, jdbcType = VARCHAR, mode = IN},
  		#{paramPatternValue, jdbcType = VARCHAR, mode = IN},
  		#{paramArrYmdStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrOddsRankStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrOddsStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrTotalRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrHitRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrIncomeRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramMetricBiasPercent, jdbcType = FLOAT, mode = IN},
  		#{paramArrBiasRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrMinusDaysRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramMinusChangedCountStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrBetCountStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentIncomeAmtStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentHitRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentIncomeRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentMinusDaysRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentIncomeAmtSlopeStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentRecoverPlusRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramIsFinal, jdbcType = VARCHAR, mode = IN},
  		#{paramIsCustom, jdbcType = VARCHAR, mode = IN}
  	)}
  </update>
  <update id="executeAnalyzeWekaKumiban" parameterType="hashmap" statementType="CALLABLE">
  	{call analyze_weka_kumiban(
  		#{paramDescription, jdbcType = VARCHAR, mode = IN},
  		#{paramBettype, jdbcType = VARCHAR, mode = IN},
  		#{paramKumiban, jdbcType = VARCHAR, mode = IN},
  		#{paramArrYmdStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrOddsRankStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrOddsStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrTotalRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrHitRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrIncomeRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramMetricBiasPercent, jdbcType = FLOAT, mode = IN},
  		#{paramArrBiasRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrMinusDaysRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramMinusChangedCountStr, jdbcType = VARCHAR, mode = IN},
  		#{paramArrBetCountStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentIncomeAmtStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentHitRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentIncomeRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentMinusDaysRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentIncomeAmtSlopeStr, jdbcType = VARCHAR, mode = IN},
  		#{paramPercentRecoverPlusRateStr, jdbcType = VARCHAR, mode = IN},
  		#{paramIsFinal, jdbcType = VARCHAR, mode = IN},
  		#{paramIsCustom, jdbcType = VARCHAR, mode = IN}
  	)}
  </update>
  <update id="executeOutputMultiWeka" parameterType="hashmap" statementType="CALLABLE">
  	{call output_multiweka(
  		#{fromYmd, jdbcType = VARCHAR, mode = IN},
  		#{toYmd, jdbcType = VARCHAR, mode = IN},
  		#{yearsCount, jdbcType = INTEGER, mode = IN},
  		#{betType, jdbcType = VARCHAR, mode = IN},
  		#{rankCols, jdbcType = INTEGER, mode = IN},
  		#{description, jdbcType = VARCHAR, mode = IN},
  		#{path, jdbcType = VARCHAR, mode = IN}
  	)}
  </update>
  <update id="executeOutputWeka" parameterType="hashmap" statementType="CALLABLE">
  	{call output_weka(
  		#{fromYmd, jdbcType = VARCHAR, mode = IN},
  		#{toYmd, jdbcType = VARCHAR, mode = IN},
  		#{yearsCount, jdbcType = INTEGER, mode = IN},
  		#{betType, jdbcType = VARCHAR, mode = IN},
  		#{rankCols, jdbcType = INTEGER, mode = IN},
  		#{description, jdbcType = VARCHAR, mode = IN},
  		#{path, jdbcType = VARCHAR, mode = IN}
  	)}
  </update>
  <update id="executeGenerateDataset49" parameterType="hashmap" statementType="CALLABLE">
  	{call generate_dataset_49(
  		#{fromYmd, jdbcType = VARCHAR, mode = IN},
  		#{toYmd, jdbcType = VARCHAR, mode = IN},
  		#{betType, jdbcType = VARCHAR, mode = IN},
  		#{betTypeName, jdbcType = VARCHAR, mode = IN},
  		#{wrWaku, jdbcType = VARCHAR, mode = IN},
  		#{datasetPath, jdbcType = VARCHAR, mode = IN}
  	)}
  </update>
</mapper>